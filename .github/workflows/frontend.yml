name: Frontend

on: [push]
jobs:
  init:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        node-varsion: [ 17.x ]
    steps:
        uses: actions/checkout@v4
      - name: Starting Node.js ${{ matrix.node-version }}
